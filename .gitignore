# Secrets — never commit
api-token
setup-token

# Diagnostic (temporary setup-token comparison)
setup-token-test.log
rate-limits-status.json

# Rich usage history + dedup sidecar (local analysis data)
usage-history.jsonl
.usage-sig
usage-windows.jsonl
.5h-reset

# Build artifacts (regenerated by build.sh)
ClaudeUsageBar.app/

# macOS junk
.DS_Store

# Runtime log, auto-refresh cooldown stamp, empty cwd for the headless CLI
updater.log
.refresh-attempt
.refresh-cwd/

# Python bytecode (py_compile checks)
__pycache__/
